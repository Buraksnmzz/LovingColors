namespace GetHint
{
    public struct GetHintPopupData
    {
        public GetHintPopupType PopupType;

        public GetHintPopupData(GetHintPopupType popupType)
        {
            PopupType = popupType;
        }
    }
}
